[project]
name = "aura-autoresearch-example"
version = "0.1.0"
description = "AURA example: automated research experiment pipeline"
requires-python = ">=3.11"
dependencies = [
    "aura",
    "anthropic",
]

[tool.uv.sources]
aura = { path = "../../", editable = true }
